Flask==2.3.2
Flask[async]==2.3.2  # 支持异步路由
gTTS==2.3.1
edge-tts==7.0.2  # 精确指定版本以提高稳定性
numpy==1.23.5
scipy==1.10.1
requests>=2.31.0  # 用于与 Docker 容器通信
docker>=6.1.3  # 用于管理 Docker 容器
protobuf>=3.20.2  # 解决依赖冲突
librosa  # Edge-TTS 可能依赖的音频处理库

# 注意：使用 PaddleSpeech 需要安装 Docker
# 安装 Docker 后，系统会自动下载并运行 PaddleSpeech 容器

# 注意：Edge-TTS 不需要下载模型，直接使用微软的在线服务